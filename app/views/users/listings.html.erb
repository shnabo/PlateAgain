<h1> Listings for <%= @user.name %> </h1>


<%= render 'listings' %>

  <ul>
    <li>
      <%= link_to listing.item_name, listing_url(listing) %><br />
      Available until: <%= listing.available_until_readable %>
      <% @listings.each do |listing| %>
      <% end %>
    </li>
  </ul>
